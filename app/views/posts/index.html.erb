<%= link_to "New Post", new_post_path %>

<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <a href="/posts/<%= post.id %>"><%= post.title %></a> by <%= post.user.full_name %> on <%= post.timestamp %>
    </li>
  <% end %>
</ul>
